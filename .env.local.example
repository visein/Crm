# Supabase Configuration
# Supabase Configuration
NEXT_PUBLIC_SUPABASE_URL=your_supabase_project_url_here
NEXT_PUBLIC_SUPABASE_ANON_KEY=your_supabase_anon_key_here

# Application Configuration
NEXT_PUBLIC_APP_NAME="Turkish CRM"
NEXT_PUBLIC_APP_VERSION="1.0.0"

# Optional: Development/Production Environment
NODE_ENV=development

# Optional: Analytics and Logging (if you add them later)
# NEXT_PUBLIC_ANALYTICS_ID=your_analytics_id
# SENTRY_DSN=your_sentry_dsn

# Database Schema Information
# Make sure your Supabase database has these tables:
# - musteriler (customers/leads)
# - satis_takip (sales pipeline & deal tracking)
# - sozlesmeler (contracts/subscriptions)
# - odemeler (payments/invoices)
# - etkilesimler (interactions/AI & human messages log)
# - operasyon_detaylari (operational settings per customer)
# - haftalik_ozet_rapor (view - weekly summary report)

# Authentication Configuration
# The app uses Supabase Auth with user metadata for roles
# Required user metadata fields: { role: 'Admin' | 'Sales' | 'Finance' | 'Operations' }

# Application Configuration
NEXT_PUBLIC_APP_NAME=Turkish CRM
NEXT_PUBLIC_APP_URL=http://localhost:3000